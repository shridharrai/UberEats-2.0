import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  page: {
    flexDirection: "row",
    alignItems: "center",
    margin: 10,
  },
  image: {
    width: 75,
    height: 75,
    marginRight: 5,
  },
  name: {
    fontWeight: "600",
    fontSize: 16,
  },
});
